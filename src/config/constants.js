export const MODES = {
  ROUND: 'ROUND',
  SWAP: 'SWAP',
  CHAIN: 'CHAIN',
  QUOTA: 'QUOTA',
};

export const ALLOWED_MIME = ['image/png','image/jpeg','image/jpg','image/webp','image/gif'];
export const SIZE_LIMIT = 8 * 1024 * 1024; // 8MB

export const DEFAULTS = {
  durationMin: null,
  quota: 5,
};
